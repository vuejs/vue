machine:
  node:
    version: 5

general:
  branches:
    only:
      - next

test:
  override:
    - npm run lint
    - npm run test:cover # only run unit tests in Phantom for now before we setup SauceLabs
    - npm run test:ssr
